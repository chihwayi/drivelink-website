import { ImageResize } from './image-resize';

describe('ImageResize', () => {
  it('should create an instance', () => {
    const directive = new ImageResize();
    expect(directive).toBeTruthy();
  });
});
